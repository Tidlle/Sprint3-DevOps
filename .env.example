# Copie este arquivo para ".env" (nao versionado) e defina suas proprias senhas
# antes de rodar "docker compose up". Nunca commite o arquivo ".env" real.
MYSQL_ROOT_PASSWORD=troque-esta-senha
MYSQL_PASSWORD=troque-esta-senha
